import { CheckfirstletterDirective } from './checkfirstletter.directive';

describe('CheckfirstletterDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckfirstletterDirective();
    expect(directive).toBeTruthy();
  });
});
